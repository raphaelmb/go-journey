name: CI

on:
    #push:
        #branches: ["main"]
    workflow_dispatch:

jobs:
    build-and-push:
        name: "Build and push"
        runs-on: ubuntu-latest

        steps:
        - name: Checkout
          uses: actions/checkout@v4
        
        - name: Build Docker image
          run: docker build -t raphaelmb/go-journey:latest -f Dockerfile.prod .  